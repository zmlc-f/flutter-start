import 'package:flutter/material.dart';
// import 'state.dart';
// import 'router.dart';
// import 'router-map.dart';
// import 'random.dart';
// import 'http.dart';
import 'dio.dart';

void main() {
  // runApp(const StateWidget());
  // runApp(const RouterWidget());
  // runApp(const RouterMapWidget());
  // runApp(const RandomWidget());
  // runApp(const HttpWidget());
  runApp(const DioWidget());
}
